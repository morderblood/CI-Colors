# Реализованные изменения: Гибкая система оптимизации гиперпараметров

## Обзор

Система рефакторинга классов гиперпараметризации для поддержки различных оптимизаторов и настраиваемых параметров.

## Созданные/Измененные файлы

### 1. **HyperparameterConfig.kt** (новый)
Конфигурация для одного гиперпараметра с:
- Имя параметра
- Начальное значение, sigma, границы
- Функция преобразования (например, `{ it.toInt() }`)

### 2. **HyperparameterSample.kt** (обновлен)
Результат оптимизации теперь содержит:
- `optimizerName`: String
- `parameters`: Map<String, Any> (вместо фиксированных полей)
- `meanError`: Double

### 3. **HyperparameterObjective.kt** (переименован из CMAESHyperparameterObjective.kt)
Теперь generic класс, который:
- Принимает `innerOptimizerName` и `hyperparameters: List<HyperparameterConfig>`
- Динамически преобразует параметры через функции `transform`
- Работает с любым оптимизатором

### 4. **HyperparameterOptimizer.kt** (переименован из CMAESHyperparameterOptimizer.kt)
Generic оптимизатор гиперпараметров:
- Принимает `innerOptimizerName` и `hyperparameters: List<HyperparameterConfig>`
- Использует CMA-ES как внешний оптимизатор
- Возвращает `HyperparameterSample` с Map параметров

### 5. **HyperparameterResultReader.kt** (обновлен)
Обновлен для работы с гибкой структурой:
- Динамически извлекает параметры из CSV
- Поддерживает любое количество параметров
- Автоматически определяет типы (Int, Double, String)

### 6. **Main.kt** (обновлен)
Примеры использования новой системы:
- `runHyperoptimization()`: Оптимизация CMA-ES
- `runNSGAIIHyperoptimization()`: Пример оптимизации NSGAII

### 7. **HyperparameterExamples.kt** (новый)
Готовые примеры для различных оптимизаторов:
- CMA-ES
- NSGAII
- SPEA2
- BOBYQA
- Powell
- Nelder-Mead

### 8. **README.md** (новый)
Полная документация с примерами использования

## Основные преимущества

### ✅ Гибкость
- Легко добавлять новые оптимизаторы
- Любое количество параметров
- Произвольные типы параметров

### ✅ Переиспользуемость
- Один класс для всех оптимизаторов
- Декларативная конфигурация

### ✅ Типобезопасность
- Функции преобразования гарантируют правильные типы
- Компиляция проверяет корректность

### ✅ Расширяемость
- Легко добавлять новые параметры
- Легко добавлять новые оптимизаторы

## Пример использования

```kotlin
// Определить параметры для оптимизации
val hyperparams = listOf(
    HyperparameterConfig(
        name = "populationSize",
        initialValue = 100.0,
        sigma = 50.0,
        lowerBound = 50.0,
        upperBound = 500.0,
        transform = { it.toInt() }  // Преобразование в Int
    ),
    HyperparameterConfig(
        name = "sigma",
        initialValue = 0.3,
        sigma = 0.1,
        lowerBound = 0.01,
        upperBound = 1.0,
        transform = { it }  // Оставить Double
    )
)

// Создать оптимизатор
val optimizer = HyperparameterOptimizer(
    trainingDataPath = "path/to/data.csv",
    numSamples = 20,
    tempOutputDir = "path/to/temp",
    innerOptimizerName = "CMA-ES",  // Любой оптимизатор!
    hyperparameters = hyperparams
)

// Запустить оптимизацию
val best = optimizer.optimize()

// Вывести результаты
println("Best parameters: ${best.parameters}")
println("Mean error: ${best.meanError}")
```

## Добавление нового оптимизатора

Чтобы оптимизировать новый оптимизатор:

1. Определите его параметры с помощью `HyperparameterConfig`
2. Создайте `HyperparameterOptimizer` с нужным `innerOptimizerName`
3. Запустите `optimize()`!

Никаких изменений в коде не требуется! 🎉

## Статус компиляции

✅ Проект успешно компилируется
✅ Все тесты проходят
✅ Нет критических ошибок
⚠️ Есть только предупреждения о неиспользуемых функциях (это нормально для примеров)

## Следующие шаги

1. Протестировать оптимизацию для различных оптимизаторов
2. Добавить параллельную оптимизацию нескольких оптимизаторов
3. Добавить сохранение результатов в базу данных
4. Визуализация результатов оптимизации

